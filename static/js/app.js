/* ============================================================================
   DOAMMO UI - Main Application Logic
   ============================================================================ */

document.addEventListener('DOMContentLoaded', () => {
    console.log('DOAMMO Narrative Engine UI initialized');

    // Initialize Lucide icons
    lucide.createIcons();

    console.log('All systems ready');
});
